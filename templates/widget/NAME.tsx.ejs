---
patch:
  append: "export * from './<%= props.kebabCaseName %>'\n"
  skip: <%= props.skipIndexFile %>
---
import React from 'react'

import { Box } from '../box'
import { Text } from '../text'
import { ViewProps } from '@/shared/types'

type Props = {
  // components's props
}

/**
 * Describe your component here
 */

export const <%= props.pascalCaseName %>: React.FC<Props & ViewProps> = function (props: Props) {
  return (
    <Box>
      <Text>HelloNe</Text>
    </Box>
  )
}
