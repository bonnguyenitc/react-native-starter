import React from 'react';
import { ActivityIndicator, ActivityIndicatorProps } from 'react-native';

export const Spinner = function (props: ActivityIndicatorProps) {
  return <ActivityIndicator {...props} />;
};
